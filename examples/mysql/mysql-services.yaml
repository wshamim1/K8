apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mysql
spec:
  type: NodePort  # Change to LoadBalancer if using cloud provider
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30006  # Optional, for NodePort type
  selector:
    app: mysql
